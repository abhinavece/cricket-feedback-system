# =============================================================================
# Environment Variables Configuration
# =============================================================================
# This file documents all environment variables used across the application.
# Copy this file to .env and fill in your actual values.
# NEVER commit .env files to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# MongoDB Configuration
# -----------------------------------------------------------------------------
# Used for Kubernetes deployments and Docker Compose setups
# For local development, see backend/.env.example
# -----------------------------------------------------------------------------
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your-secure-password-change-this
MONGO_DATABASE=cricket-feedback

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------
# NODE_ENV: Environment mode (development, production, test)
# JWT_SECRET: Secret key for JWT token signing (use a strong random string)
#   Generate with: openssl rand -base64 32
# GOOGLE_CLIENT_ID: OAuth 2.0 client ID from Google Cloud Console
# GOOGLE_CLIENT_SECRET: OAuth 2.0 client secret from Google Cloud Console
# SUPER_ADMIN_KEY: Key for initial admin setup (change in production)
# -----------------------------------------------------------------------------
NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-change-in-production
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
SUPER_ADMIN_KEY=super-admin-setup-key-2024

# -----------------------------------------------------------------------------
# Frontend Configuration
# -----------------------------------------------------------------------------
# REACT_APP_API_URL: Base URL for backend API (no trailing slash)
#   Development: http://localhost:5000/api
#   Production: https://your-domain.com/api
# REACT_APP_GOOGLE_CLIENT_ID: Same as backend GOOGLE_CLIENT_ID
# -----------------------------------------------------------------------------
REACT_APP_API_URL=http://localhost/api
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id

# -----------------------------------------------------------------------------
# WhatsApp Cloud API Configuration (Optional)
# -----------------------------------------------------------------------------
# WHATSAPP_ACCESS_TOKEN: Access token from Meta for Developers
# WHATSAPP_PHONE_NUMBER_ID: Phone number ID from Meta
# WHATSAPP_VERIFY_TOKEN: Token for webhook verification (set in Meta dashboard)
# -----------------------------------------------------------------------------
# WHATSAPP_ACCESS_TOKEN=your-whatsapp-access-token
# WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
# WHATSAPP_VERIFY_TOKEN=your-verify-token

# -----------------------------------------------------------------------------
# AI Service Configuration (Optional)
# -----------------------------------------------------------------------------
# GOOGLE_AI_STUDIO_API_KEY: API key for Google AI Studio (Gemini)
# AI_SERVICE_ENABLED: Enable/disable AI service (true/false)
# DAILY_REQUEST_LIMIT: Maximum requests per day (default: 100)
# MIN_CONFIDENCE_THRESHOLD: Minimum confidence for AI results (0.0-1.0)
# -----------------------------------------------------------------------------
# GOOGLE_AI_STUDIO_API_KEY=your-google-ai-studio-api-key
# AI_SERVICE_ENABLED=true
# DAILY_REQUEST_LIMIT=100
# MIN_CONFIDENCE_THRESHOLD=0.7

# -----------------------------------------------------------------------------
# Feature Flags (Optional)
# -----------------------------------------------------------------------------
# Control gradual rollout of new features.
# Set to 'true' to enable globally, or use *_USERS/*_ORGS for allowlists.
#
# FF_MULTI_TENANT: Enable multi-tenant system (team discovery, join requests)
# FF_MULTI_TENANT_USERS: Comma-separated list of beta tester emails
# FF_MULTI_TENANT_ORGS: Comma-separated list of beta org IDs
#
# FF_TEAM_DISCOVERY: Enable team search and join requests
# FF_TEAM_DISCOVERY_USERS: Comma-separated list of beta tester emails
#
# FF_WHATSAPP_BYOT: Enable custom WhatsApp number for teams
# FF_WHATSAPP_BYOT_ORGS: Comma-separated list of org IDs with BYOT access
# -----------------------------------------------------------------------------
# FF_MULTI_TENANT=false
# FF_MULTI_TENANT_USERS=beta@example.com,tester@example.com
# FF_MULTI_TENANT_ORGS=
# FF_TEAM_DISCOVERY=false
# FF_TEAM_DISCOVERY_USERS=
# FF_WHATSAPP_BYOT=false
# FF_WHATSAPP_BYOT_ORGS=
